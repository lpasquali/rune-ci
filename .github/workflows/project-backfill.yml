# SPDX-License-Identifier: Apache-2.0
name: Project Board Backfill

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *"

permissions:
  contents: read

jobs:
  backfill:
    uses: lpasquali/rune-ci/.github/workflows/project-backfill-logic.yml@23b84477724ddf4c91fbbe1535657639f921d18c
    secrets:
      PROJECT_TOKEN: ${{ secrets.PROJECT_TOKEN }}
