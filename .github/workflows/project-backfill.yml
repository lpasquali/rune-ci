# SPDX-License-Identifier: Apache-2.0
name: Project Board Backfill

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *"

permissions:
  contents: read

jobs:
  backfill:
    uses: lpasquali/rune-ci/.github/workflows/project-backfill-logic.yml@cfdb81c2d4f03bb82786729401fe7b8e833e4347
    secrets:
      PROJECT_TOKEN: ${{ secrets.PROJECT_TOKEN }}
