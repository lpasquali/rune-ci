# SPDX-License-Identifier: Apache-2.0
name: Project Board Sync

on:
  issues:
    types: [opened, closed, reopened, labeled, unlabeled, assigned, unassigned]
  pull_request:
    types: [opened, closed, reopened, ready_for_review, converted_to_draft, labeled, unlabeled, synchronize]

permissions:
  contents: read

jobs:
  sync:
    uses: lpasquali/rune-ci/.github/workflows/project-sync-logic.yml@a0f2e64a22021e9c9359f3679a0957aafd200138
    secrets:
      PROJECT_TOKEN: ${{ secrets.PROJECT_TOKEN }}
