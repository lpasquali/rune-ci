# SPDX-License-Identifier: Apache-2.0
# Stub — implementation in rune-docs#130
name: "Gitleaks Secret Scan"
description: "Run Gitleaks secret scanning against the repository"
inputs:
  gitleaks-version:
    description: "Gitleaks binary version"
    required: false
    default: "8.24.3"
runs:
  using: "composite"
  steps:
    - shell: bash
      run: echo "::error::Stub action — not yet implemented (rune-docs#130)"
