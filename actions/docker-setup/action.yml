# SPDX-License-Identifier: Apache-2.0
# Stub — implementation in rune-docs#133
name: "Docker Login + Buildx Setup"
description: "Authenticate to GHCR and configure Docker Buildx with registry caching"
inputs:
  registry:
    description: "Container registry"
    required: false
    default: "ghcr.io"
  registry-owner:
    description: "Registry owner/namespace"
    required: true
  image-name:
    description: "Image name (without registry prefix)"
    required: true
runs:
  using: "composite"
  steps:
    - shell: bash
      run: echo "::error::Stub action — not yet implemented (rune-docs#133)"
