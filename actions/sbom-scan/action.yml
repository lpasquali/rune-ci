# SPDX-License-Identifier: Apache-2.0
# Stub — implementation in rune-docs#131
name: "SBOM + CVE Scan"
description: "Generate SBOM (Syft) and scan for vulnerabilities (Grype, Trivy)"
inputs:
  image:
    description: "Container image to scan"
    required: true
  cvss-threshold:
    description: "CVSS score threshold for blocking"
    required: false
    default: "7.0"
  vex-file:
    description: "Path to OpenVEX file for suppression"
    required: false
    default: ".vex/permanent.openvex.json"
runs:
  using: "composite"
  steps:
    - shell: bash
      run: echo "::error::Stub action — not yet implemented (rune-docs#131)"
